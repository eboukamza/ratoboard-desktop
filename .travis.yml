language: node_js
node_js:
- '8'
matrix:
  include:
  - os: osx

cache:
  yarn: true
script:
  - yarn install --force
  - ls -alb node_modules/iohook/builds
  - yarn run build --prod --no-progress
  - yarn release
notifications:
  email: false
branches:
  except:
    - "/^v\\d+\\.\\d+\\.\\d+$/"
